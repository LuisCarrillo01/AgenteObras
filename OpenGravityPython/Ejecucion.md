en la carpeta del proyecto del agente ejecutar los siguientes comandos:

# Ejemplo de ruta:

cd "C:\Users\Andresl\Documents\Agente\OpenGravityPython"

python -m venv .venv
.\.venv\Scripts\Activate.ps1

# Instalar dependencias
pip install -r requirements.txt

# Ejecutar el bot
python main.py
